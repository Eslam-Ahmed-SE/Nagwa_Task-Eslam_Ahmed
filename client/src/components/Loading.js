//basic splash screen that tells the user info about the app while the word list gets loaded from the node.js server 
function Loading() {

    return (
        <div>
            <h1>You will be presented with 10 words</h1>
            <h2>choose the correct part of speach</h2>
        </div>
    );
}

export default Loading;